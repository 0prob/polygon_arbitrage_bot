/**
 * Global constants for the arbitrage engine.
 * Re-exported from the centralized source of truth in hyperindex.
 */
import {
  RATE_PRECISION,
  BPS_DENOM,
  BPS_DENOMINATOR,
  MAJOR_TOKEN_DATA,
  MAJOR_TOKENS,
  MAJOR_TOKEN_APPROX_RATES,
} from "../../hyperindex/src/utils/constants.ts";

export {
  RATE_PRECISION,
  BPS_DENOM,
  BPS_DENOMINATOR,
  MAJOR_TOKEN_DATA,
  MAJOR_TOKENS,
  MAJOR_TOKEN_APPROX_RATES,
};
