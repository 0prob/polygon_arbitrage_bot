/**
 * src/app/index.ts — Barrel export for app layer
 *
 * NOTE: This barrel is intentionally empty. Individual app modules are
 * imported directly by runner.ts and other consumers to avoid circular
 * dependencies and maintain explicit import paths.
 */