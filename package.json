{
  "name": "polygon-arb-bot",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "main": "src/cli/main.ts",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "hyperindex",
    ".opencode"
  ],
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "start": "bun --env-file=.env run src/cli/main.ts",
    "tui": "bun --env-file=.env run src/cli/main.ts --tui",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun --bun run vitest run",
    "test:coverage": "bun --bun run vitest run --coverage",
    "lint": "bunx eslint src/ --fix",
    "fmt": "bunx prettier --write src/",
    "compile": "cd hyperindex && npx tsc --outDir src/handlers_mjs --skipLibCheck --moduleResolution bundler; true",
    "dev": "cd hyperindex && bunx envio dev",
    "cgen": "cd hyperindex && bunx envio codegen"
  },
  "dependencies": {
    "envio": "^3.0.2",
    "pino": "^10.3.1",
    "viem": "^2.51.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@vitest/coverage-istanbul": "^4.1.7",
    "@vitest/coverage-v8": "^4.1.7",
    "bun-types": "^1.3.14",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "fast-check": "^4.8.0",
    "pino-pretty": "^13.1.3",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.4",
    "vitest": "^4.1.7"
  }
}
